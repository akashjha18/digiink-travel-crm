# Deployment (not yet implemented — notes for later phases)

Not built in Phase 1: CI/CD pipeline, infra-as-code for automated tenant
DB provisioning in production (Terraform), PgBouncer-equivalent
connection pooling for MySQL, CDN for frontend static assets, Sentry/
uptime monitoring. These are called out in the SRS as production
requirements but are out of scope until the app runs correctly locally
end-to-end.
